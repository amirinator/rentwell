/**
 * @rentwell/graphql
 *
 * The API contract: the SDL, the custom scalars that guard its edges, and the
 * TypeScript types shared by the server and the web client.
 */

export * from './schema';
export * from './scalars';
export * from './types';
